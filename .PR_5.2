5.2. 
#include <iostream>
using namespace std;

class Complex {
public:
    int realPart, imaginaryPart;

    void input() {
        cout << "\nEnter the real part: ";
        cin >> realPart;
        cout << "Enter the imaginary part: ";
        cin >> imaginaryPart;
    }

    void operator+(Complex &other) {
        Complex result;
        result.realPart = realPart + other.realPart;
        result.imaginaryPart = imaginaryPart + other.imaginaryPart;
        cout << "Sum: " << result.realPart << "+" << result.imaginaryPart << "i" << endl;
    }
};

int main() {
    Complex complex1, complex2;
    complex1.input();
    complex2.input();
    complex1 + complex2;
    cout << "24CE021_SMIT_DAFDA ";
    return 0;
}
